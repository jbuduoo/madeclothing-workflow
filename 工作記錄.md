# 工作記錄

## 專案狀態

目前檢查到的「成衣後台」檔案，內容是 Vue 前端專案打包後的 `dist` 成品，不是完整原始碼。

已確認前端是 Vue 3 + Vue Router + Axios 寫成，打包檔中可從 source map 看到：

- `src/App.vue`
- `src/components/LoginPage.vue`
- `src/components/UserDashboard.vue`
- `src/components/EditPage.vue`
- `src/router/index.js`
- `src/main.js`

目前本機有兩份相同內容：

- `MadeClothing/dist`
- `成衣後台/dist`

另有壓縮檔：

- `back/成衣後台.zip`
- `back/MadeClothing.rar`

兩個壓縮檔內容也都只有 `dist`，沒有完整 Vue 專案原始碼，也沒有後端程式碼。

## 現有前端呼叫的後端 API

前端目前呼叫 Google Cloud Functions：

- `https://us-central1-nft-linegame.cloudfunctions.net/function-1/login`
- `https://us-central1-nft-linegame.cloudfunctions.net/function-1/dashboard-data`
- `https://us-central1-nft-linegame.cloudfunctions.net/function-1/get-workflow-steps/{orderId}`
- `https://us-central1-nft-linegame.cloudfunctions.net/function-1/get-completed-records/{orderId}`
- `https://us-central1-nft-linegame.cloudfunctions.net/function-1/update-workflow-step`

判斷資料不是前端直接讀 Google 試算表，而是前端呼叫後端 API，由後端讀寫 Google Sheets。

## Google 試算表

試算表連結：

`https://docs.google.com/spreadsheets/d/13oGgQtPK6ymU4oF2RkUZBnoS3ts-Gjig7pw4ho1mv4Q/edit?usp=sharing`

已確認可讀，試算表名稱：

`晶輝企業webe後台`

頁籤：

- `Key單`
- `完成記錄`
- `Account`
- `工作表4`

目前說明：

- `Key單`：前台可看到的訂單資訊，包含圖片、已完成工序等。
- `完成記錄`：已完成的訂單流程紀錄。
- `Account`：登入帳號與密碼。
- `工作表4`：用途尚未確認。

重要風險：

- `Account` 內帳號密碼為明碼存放，建議重建系統時改掉。
- 若試算表擁有者是前工程師個人帳號，後續有權限與資產歸屬風險。

## Google Drive 圖片資料夾

圖片資料夾連結：

`https://drive.google.com/drive/folders/16EihcVopeBwXWzyAQTINLJMfHo_Nc6G9?usp=sharing`

已確認資料夾可讀，內含多個訂單圖片檔，例如 `.png`、`.jpg`。

仍需確認：

- 是否包含所有訂單圖片。
- 是否具備下載或移轉權限。
- 圖片擁有者是否為公司可控帳號。

## 測試帳號

目前取得測試帳號：

- 帳號：`admin`
- 密碼：`123456`

此帳密同時也出現在試算表 `Account` 中，屬於明碼密碼。

## 網域與 DNS

目前取得入口：

- Cloudflare：
  `https://dash.cloudflare.com/34361e2e2b398f8f6e8d1f3b251deb97/0800258758.com.tw`
- 網域管理平台：
  `https://itn.tw/clientarea.php?action=domains`

仍需確認：

- 是否已取得登入權限。
- 是否可管理 `0800258758.com.tw` 的 DNS。
- 是否可修改 Cloudflare 設定。
- 是否有 SSL 憑證設定。

## 目前仍缺的關鍵資料

1. 前端 Vue 專案完整原始碼
   - 需要 `src`
   - 需要 `package.json`
   - 需要 Vue 設定檔，例如 `vue.config.js` 或 `vite.config.js`
   - 目前只有 `dist`，不足以算完整交接。

2. Google Cloud Functions 後端完整原始碼
   - 需要 `login`
   - 需要 `dashboard-data`
   - 需要 `get-workflow-steps`
   - 需要 `get-completed-records`
   - 需要 `update-workflow-step`

3. 部署文件
   - 前端如何打包
   - 前端部署到哪裡
   - 後端如何部署
   - 環境變數或服務帳號設定

4. 主機資訊
   - 是否真的有中華電信主機尚未確認。
   - 若有，需要登入方式、網站目錄、更新檔案方式。

## 初步判斷

目前資料層大致已取得，包含 Google Sheets 和 Google Drive 圖片資料夾。

但程式層尚未完整交接。若拿不到前端原始碼與後端原始碼，後續比較像是依照現有資料與畫面重新建一套同功能系統，而不是直接接手原系統維護。

可重建，但會需要重新實作：

- 登入
- 讀取訂單列表
- 顯示 Google Drive 圖片
- 搜尋客戶
- 讀取流程狀態
- 更新流程狀態
- 寫入完成紀錄
- 管理帳號

## 建議給對方的資料請求文字

請提供此系統後續維護與移轉所需資料：

1. 前端 Vue 專案完整原始碼  
   請提供完整專案，不只是 `dist` 打包檔，需包含 `src`、`package.json`、設定檔。

2. Google Cloud Functions 後端完整原始碼  
   包含 `login`、`dashboard-data`、`get-workflow-steps`、`get-completed-records`、`update-workflow-step` 等 API。

3. Google 試算表欄位說明  
   請說明各頁籤與欄位用途，尤其是 `Key單`、`完成記錄`、`Account`。

4. Google Drive 訂單圖片資料夾連結與下載/移轉權限  
   請確認所有訂單圖片是否都在同一資料夾，並提供權限。

5. 目前網站部署位置  
   請說明網站目前放在哪裡，例如中華電信主機、GitHub Pages、Firebase Hosting、Cloud Storage、其他主機。若有主機，請提供登入方式、網站放置目錄、更新檔案方式。

6. 網域與 DNS 管理資訊  
   包含網域管理平台、DNS 設定、SSL 憑證設定方式。

7. 部署文件或簡單操作說明  
   包含前端如何打包、如何上傳部署、後端如何部署。

8. 測試帳號密碼  
   請提供可用於測試登入與流程操作的帳號。

以上資料是為了後續系統維護、移轉與備份使用，麻煩提供完整交接資料。
